
# Multilingual Voice Assistant (Final)

النسخة النهائية من مشروع المساعد الصوتي — يدعم Arabic/English Wake Word، جلسات مستمرة، وملفات corpus محلية.

**ملفات المشروع المرفقة:** `/mnt/data/Speech Recognition project.docx` . fileciteturn0file0

---

## 📌 نظرة عامة
هذا المشروع هو تطبيق مساعد صوتي ثنائي اللغة (عربي/إنجليزي) مبني بـ Python. يستخدم مكتبة `speech_recognition` لتحويل الكلام إلى نص و`gTTS` لتوليد الكلام، ويعرض واجهة مستخدم رسومية باستخدام `tkinter`. يحتوي التطبيق على وضع "Wake Word" لجلسات مستمرة، نظام corpus محلي (نكات، تحيات، حقائق، و`favorites_ar`) وبعض أوامر النظام لفتح مواقع الويب.

---

## ✨ المميزات الرئيسية
- التعرف على الكلام باللغتين العربية والإنجليزية (Google STT عبر speech_recognition)
- توليد صوتي (gTTS) و تشغيل باستخدام `pygame` عبر طابور صوتي غير متزامن
- وضع **Wake Word**: استمع لعبارة مثل "يا مساعد" أو "hey assistant" لبدء جلسة تفاعلية
- جلسة مستمرة مع انتهاء تلقائي بعد فترة خمول (قابل للضبط)
- محرك corpus محلي (الأسئلة عن "أفضل دكتور" يستخدم `favorites_ar`)
- فتح مواقع شهيرة عبر أوامر صوتية (فيسبوك، يوتيوب، انستجرام، واتساب ويب، الخ)
- دعم تجريبي لتكامل Google Gemini (عبر مفتاح API اختياري)

---

## 📁 بنية الملفات المتوقعة
```
project-root/
├─ app_final_final.py          # ملف التطبيق الرئيسي (النسخة النهائية المرسلة)
├─ corpus/
│  ├─ jokes_ar.txt
│  ├─ jokes_en.txt
│  ├─ facts_ar.txt
│  ├─ facts_en.txt
│  ├─ greetings_ar.txt
│  ├─ greetings_en.txt
│  └─ favorites_ar.txt        # مهم — يحتوي اقتراحات "أحسن دكتور" بالعربية
├─ .env                        # (اختياري) GEMINI_API_KEY=your_key_here
├─ requirements.txt            # سيتم إنشاؤه أدناه
└─ README.md
```

> **ملاحظة:** الملف `Speech Recognition project.docx` يحتوي على وصف تفصيلي للمشروع ومواصفاته وتم رفعه إلى المسار `/mnt/data/Speech Recognition project.docx`. fileciteturn0file0

---

## 🛠 المتطلبات (Requirements)
يفضل إنشاء بيئة افتراضية ثم تثبيت الحزم التالية (نماذج للأوامر):

```bash
python -m venv venv
source venv/bin/activate      # على Linux/macOS
venv\Scripts\activate.bat     # على Windows

pip install -r requirements.txt
```

**مكتبات رئيسية مستخدمة:**

- Python 3.8+
- speechrecognition
- gTTS
- pygame
- python-dotenv
- tk (عادة مضمّن مع بايثون على أغلب المنصات)

يمكنك إنشاء ملف `requirements.txt` بهذه التشكيلة الأولية:
```
SpeechRecognition>=3.8.1
gTTS>=2.3.0
pygame>=2.1.0
python-dotenv>=1.0.0
```
(أضف أو عدّل حسب بيئتك)

---

## ⚙️ الإعداد (Configuration)
1. ضع ملفات corpus داخل مجلد `corpus/` في جذر المشروع (انظر بنية الملفات أعلاه).
2. لإن أردت استخدام Google Gemini أضف `.env` في الجذر واحفظ المفتاح:
```
GEMINI_API_KEY=your_gemini_key_here
```
3. تأكد من أن الميكروفون يعمل وأن صلاحيات الوصول مسموحة لنظام التشغيل.

---

## ▶️ التشغيل (Run)
لتشغيل التطبيق:
```bash
python app_final_final.py
```

**سلوك التشغيل:**

- اضغط `Start Listening` لبدء الاستماع المستمر للعبارات العادية.
- فعّل `Wake Word Mode` لكي يبدأ الاستماع فقط بعد نطق كلمة الاستيقاظ مثل "يا مساعد".
- أثناء الجلسة يمكنك قول أوامر مثل "افتح يوتيوب" أو "مين أحسن دكتور" أو أسئلة عامة؛ إذا لم يتعرف على الأمر سيستخدم Gemini (إن كانت مفعلة) للرد الذكي.
- لوقف الجلسة قول كلمات الإيقاف مثل "خلاص" أو "باي" أو اضغط Stop.

---

## 🧩 التفاصيل الفنية وملحوظات مهمة
- **حذف ملفات TTS مؤقتًا:** النظام ينشئ ملفات مؤقتة mp3 ويزيلها بعد التشغيل.
- **إدارة الثريد:** يوجد ثريد تشغيل للصوت وثريد استماع منفصل للحفاظ على تفاعل الواجهة.
- **كشف اللغة:** يعتمد على فحص نطاق Unicode للحروف العربية لتحديد اللغة (`\u0600`–`\u06FF`).
- **ملفات Corpus:** إذا لم تتوفر ملفات corpus، يقوم البرنامج بإنشاء قائمة فارغة افتراضية ويعمل بدون أخطاء.
- **Gemini:** التكامل اختياري؛ في حال عدم تواجد `GEMINI_API_KEY` سيعمل النظام محليًا فقط.

---

## 🐞 استكشاف الأخطاء وإصلاحها (Troubleshooting)
- **pygame.mixer.init() خطأ**: تأكد من أن نظامك يحتوي على محرّك صوت (ALSA/PortAudio) وعلى تشغيل sound server إن لزم.
- **SpeechRecognition لا يتعرف على الميكروفون**: جرّب تغيير جهاز الميكروفون الافتراضي أو منح صلاحيات الوصول في الإعدادات.
- **gTTS يفشل بدون اتصال إنترنت**: gTTS يعتمد على خدمة Google TTS عبر الإنترنت؛ توفّر اتصال إنترنت صالح مطلوب.
- **Gemini fails**: تحقق من صحة `GEMINI_API_KEY` وإصدار المكتبة `genai` إذا استُخدم.

---

## 🔐 الأمن والخصوصية
- **لا تحفظ المفاتيح في المستودع** — استخدم `.env` فقط.
- تذكّر أن خدمات STT/TTS عبر الإنترنت ترسل بيانات (الصوت/النص) للمعالجين الخارجيين — راجع سياسات الخصوصية قبل الاستخدام الحساس.

---

## 👥 المساهمون
- Martin Emad Maher
- Yousef Emad
- Mohammed Abdalla
- Kirlos Ayad Libeb

Supervisor: Dr. Abeer Hassan (Department Of Artificial Intelligence)

---

## 📚 مراجع
- المستند التفصيلي المرفق في المشروع: `/mnt/data/Speech Recognition project.docx`. fileciteturn0file0
